<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>profile  </title>
</head>
<body>
    <h1>welcome <%= user.name %></h1>
    <h1>your email  <%= user.email %></h1>
    <h1>from <%= user.city %></h1>
    <ul>
        <!-- this will display the array which is coming from skills and foreach will run a loop -->
        <% user.skill.forEach((item)=>{ %>
            <li> <%= item %></li>
        <% }) %>
    </ul>
</body>
</html>